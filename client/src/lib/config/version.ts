/**
 * Application version from package.json, injected at build time via Vite.
 * Use this for displaying the deployed application version (e.g., in footer).
 * 
 * Note: This is different from changelog versions which track feature releases.
 */
export const APP_VERSION: string = __APP_VERSION__;
