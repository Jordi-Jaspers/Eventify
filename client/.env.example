# Backend API Base URL
VITE_SERVER_BASE_URL=http://localhost:8080

# Show dev credentials on login page (set to 'true' for dev/test environments)
# This is a runtime variable - can be changed without rebuilding
# Safe: Backend endpoint only exists when NOT in 'prd' profile
PUBLIC_SHOW_DEV_CREDENTIALS=false
